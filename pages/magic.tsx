import { NextPage } from 'next';

const Page: NextPage = () => (
  <main>
    If getInitialProps() is needed, we need to use next.js's exported types for React. See{' '}
    <a href="https://nextjs.org/docs#exported-types">https://nextjs.org/docs#exported-types</a>
  </main>
);

export default Page;
